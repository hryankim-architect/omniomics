# data (large / downloaded / private) — reproducible via run_golden.py & omniomics-prepare-brca
data/
*.tsv.gz

# python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
.ipynb_checkpoints/

# generated outputs / provenance
golden/audit.log
docbuild/

# os
.DS_Store

# local node-handoff scaffolding (per-round commit scripts + handoff notes; not deliverables)
commit-*.sh
HANDOFF-*.md

# internal-review slide decks (not committed deliverables)
reports/omniomics_*.pptx
